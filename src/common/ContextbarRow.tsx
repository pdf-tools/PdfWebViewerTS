import { h, Component } from 'hyperapp'

/** @internal */
export const ContextbarRow: Component<{}> = ({}, children) => {
  return (
    <div class="pwv-contextbar-row">
      {children}
    </div>
  )
}
